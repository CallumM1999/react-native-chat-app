import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    page: {
        height: '100%',
        display: 'flex',
        backgroundColor: '#fff'
    },
    container: {
        width: '100%',
        overflow: 'hidden',
        flex: 1,
    }
});

export default styles;